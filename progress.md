# Progress

## 2026-09-07
- Read the applicable brainstorming, planning, TDD, and verification workflows.
- Confirmed the current workspace is empty and located the existing reimbursement app in the prior project directory.
- Recorded the requested authentication, user administration, single-session, and per-user history scope.
- User selected option A: build the upgraded copy in the current workspace and preserve the old app.
- Inspected current HTTP, frontend, configuration, packaging, and test structure; identified that all protected resources currently bypass authentication.
- User chose one-time first-run administrator setup in the browser.
- User chose single-session policy B: a new ordinary-user login invalidates the old session.
- User chose administrator history scope B: administrators cannot view or download ordinary-user history.
- User chose not to migrate existing unowned generated files.
- Requirement changed: ordinary users self-register from the login page and require administrator approval.
- User chose registration fields A: username, password, real name, and department are required.
- User chose locked profile fields: ordinary users cannot edit traveler or department on a reimbursement form.
- User chose password policy A: self-service password change plus administrator temporary-password reset with forced change.
- User chose rejection policy B: delete the application and require a fresh registration.
- User chose session duration C: remember login for 7 days unless explicitly revoked.
- User chose administrator concurrency B: administrators are also limited to one active session.
- User chose approved-user management A: disable/enable and password reset, with no account/history deletion.
- User chose history action B: ordinary users may delete their own records and files.
- User chose recoverable deletion A: 30-day recycle bin with restore and permanent-delete actions.
- User chose profile-edit policy C: only administrators can edit approved real name and department.
- Deployment target clarified: local Mac development, then Docker on Synology NAS.
- Deployment hardware confirmed as Synology DS925+ / AMD Ryzen V1500B (`linux/amd64`), LAN-only.
- User selected implementation approach A: keep the standard-library Python server and add focused SQLite/auth/storage modules.
- User approved the architecture and module boundaries.
- User approved the SQLite data model and 30-day soft-delete model.
- User approved the registration, approval, single-session, password, CSRF, and login-security flows.
- User approved server-side history ownership checks, concurrent generation isolation, downloads, and recycle-bin behavior.
- User selected visual layout B: left sidebar for ordinary-user and administrator navigation.
- User approved Docker persistence, backup, error handling, and verification design; default port changed to 8800.
- Wrote and self-reviewed `docs/superpowers/specs/2026-09-07-auth-user-history-design.md`; awaiting user approval before implementation planning.
- User approved the written design specification.
- Wrote and self-reviewed `docs/superpowers/plans/2026-09-07-auth-user-history.md`; all design requirements map to implementation and verification tasks.
- Task 1 complete: copied the verified baseline into a new Git repository, restored the receipt fixture, made tests use the repository template, and verified all 57 baseline tests pass with the bundled runtime.
- Task 2 complete: added typed Mac/Docker configuration, exact versioned SQLite schema, transaction/backup helpers, strict type validation, and connection cleanup; 16 focused tests pass.
- Task 3 complete: added bounded scrypt password hashing, atomic persistent secret creation, and signed anonymous CSRF tokens; 22 focused security tests pass, including fault-injected concurrency and resource-boundary cases.
- Task 4 complete: added one-time administrator setup, self-registration, approval/rejection, profile/status/password administration, and concurrency-safe compensating revocation; 30 user/database tests pass.
- Task 5 complete: added opaque single-session authentication with absolute 7-day expiry, hashed tokens, CSRF validation, active-account checks, and deterministic revocation. Session issuance is bound to the authenticated user's password/status version snapshot so concurrent password or status changes cannot revive stale credentials; 48 session/user/database tests pass, and both specification and code-quality reviews approved it.
- Task 6 complete: added guarded setup/login/registration/session/password/logout routes, ordered setup/session/status/forced-change/role/CSRF guards, secure session cookies, protected static handling, bounded request concurrency and idle timeouts, parser-safe JSON errors, authentication timing equalization, shared scrypt admission control, and per-key anonymous rate limits. The full 166-test core suite and the real workbook-to-PDF end-to-end test pass with resource warnings treated as errors; Task 7 has not started.
- Task 6 specification follow-up started: reproduce and fix callback-level disconnect handling and bodyless saturated HEAD responses with strict TDD, then remove duplicated throttle/scrypt expectations from Task 9 documentation. Task 7 remains untouched.
- Task 6 specification follow-up RED: callback `TimeoutError` attempts a JSON 500 while all callback disconnect types leave the connection open; a real saturated `HEAD /healthz` response includes the 503 JSON body. The ordinary callback `RuntimeError` safe-500 characterization already passes.
- Task 6 specification follow-up GREEN: callback `TimeoutError`/`ConnectionError`/`BrokenPipeError` now close and return without a second response while ordinary exceptions retain safe JSON 500 handling; saturated HEAD returns 503 headers and declared JSON length without transmitting a body. Task 9 now treats Task 6 throttling and shared scrypt admission as existing regression coverage rather than new failing work.
- Task 6 specification follow-up verification: 43 affected HTTP tests and the complete 166-test core suite pass with `ResourceWarning` treated as errors; the real workbook-to-PDF end-to-end test, syntax compilation, and diff check also pass.
- Self-review clarified pre-setup route restrictions, protected administrator targets, and the stopped-service one-shot backup flow.
- Task 6 saturation-framing follow-up started: add deterministic coverage for a saturated socket accepted before GET bytes are readable, plus partial `H`/`HE`/`HEA` method prefixes, then require complete JSON framing unless `HEAD ` is fully confirmed.
- Task 6 saturation-framing RED: the delayed real GET and each empty/partial HEAD prefix produced an empty body that could not be decoded as JSON, confirming the response was incorrectly classified as HEAD.
- Focused verification exposed that the prior live HEAD test raced the server's nonblocking peek. The confirmed-HEAD contract now uses a socket pair with the complete method token buffered before rejection, while the live saturated delayed-GET test retains coverage of the accept-before-readable path.
- Task 6 saturation-framing GREEN: rejection now suppresses its body only for an observed complete `HEAD ` token. The focused 5-test saturation set and all 45 HTTP tests pass, including exact JSON `Content-Length` checks for delayed GET and confirmed HEAD behavior.
- Task 6 saturation-framing verification: the complete 169-test suite passes with `ResourceWarning` treated as errors; the standalone real workbook-to-PDF end-to-end test passes 1/1; syntax compilation and diff checks pass. Task 7 remains untouched.
